             {/* First Section */}
                                    <div className='flex flex-row'>
                                        <ScheduleMaker position={1} weeklySchedule={weeklySchedule} setWeeklySchedule={setWeeklySchedule} />
                                    </div>

                                    {/* Second Section */}
                                    {
                                        secondSection && <div className='flex flex-row'>
                                            <ScheduleMaker position={2} weeklySchedule={weeklySchedule} setWeeklySchedule={setWeeklySchedule} />
                                            {/* Delete Button if extra optional section*/}
                                            <div className="pl-2 pt-3">
                                                <FaXmark
                                                    className="text-lg cursor-pointer hover:bg-red-300 hover:rounded-lg"
                                                    onMouseDown={() => {
                                                        const newSched = weeklySchedule.filter(obj => obj["position"] !== 2)
                                                        setSecondSection(false)
                                                        setWeeklySchedule(newSched)
                                                    }}
                                                />
                                            </div>
                                        </div>
                                    }

                                    {/* Third Section */}
                                    {
                                        thirdSection && <div className='flex flex-row'>
                                            <ScheduleMaker position={3} weeklySchedule={weeklySchedule} setWeeklySchedule={setWeeklySchedule} />
                                            {/* Delete Button if extra optional section*/}
                                            <div className="pl-2 pt-3">
                                                <FaXmark
                                                    className="text-lg cursor-pointer hover:bg-red-300 hover:rounded-lg"
                                                    onMouseDown={() => {
                                                        const newSched = weeklySchedule.filter(obj => obj["position"] !== 3)
                                                        setThirdSection(false)
                                                        setWeeklySchedule(newSched)
                                                    }
                                                    }
                                                />
                                            </div>
                                        </div>
                                    }